
def get_embedding(text: str) -> list:
    # Placeholder for embedding function
    # Replace with actual model (e.g., sentence-transformers or OpenAI API)
    import random
    return [random.uniform(-1, 1) for _ in range(384)]
